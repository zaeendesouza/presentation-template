% General Notes %
% 1. User needs to first install fonts in folder
% 2. Then go to Options -> Configure -> Build -> Compile with Xelatex
% 3. Compile after installing all packages needed. Will give errors in first try. Just compile again.

% Slide settings, packages and link options.
\documentclass[11pt, aspectratio=169]{beamer} % remove aspectratio line for standard beamer size (square slides)
\usepackage{appendixnumberbeamer}
\usepackage{booktabs}
\usepackage{threeparttable}
\setlength\belowcaptionskip{-2pt}
\usetheme[progressbar = frametitle,
numbering = fraction]{metropolis}

% Colours and Links: Setup
\definecolor{cseblue}{RGB}{22,146,109}
\definecolor{Arsenic}{RGB}{26, 41, 44}
\setbeamercolor{frametitle}{bg = white, fg = Arsenic}
\setbeamercolor{alerted text}{fg = cseblue}
\setbeamercolor{background canvas}{bg = white}
\hypersetup{colorlinks,
	citecolor = cseblue,
	filecolor = Arsenic,
	linkcolor = Arsenic,
	urlcolor = cseblue}
\setbeamercolor{progress bar}{fg = cseblue}
\setbeamercolor{block title}{bg = white}
\newcommand{\themename}{\textbf{\textsc{metropolis}}\xspace}

% Formatting
\makeatletter
\setbeamertemplate{title page}{
	\begin{minipage}[b][\paperheight]{\textwidth}
		\centering
		\vfill%
		\ifx\inserttitle\@empty\else\usebeamertemplate*{title}\fi
		\ifx\insertsubtitle\@empty\else\usebeamertemplate*{subtitle}\fi
		\usebeamertemplate*{title separator}
		\ifx\beamer@shortauthor\@empty\else\usebeamertemplate*{author}\fi
		\ifx\insertdate\@empty\else\usebeamertemplate*{date}\fi
		\ifx\insertinstitute\@empty\else\usebeamertemplate*{institute}\fi
		\vfill%
		\ifx\inserttitlegraphic\@empty\else\inserttitlegraphic\fi
		%   <---------ADD MORE LOGOS HERE--------------->
		% if you need an > 1 logo un-comment this line below, duplicate n times and then update link under author name section
		%\hfill
		%\ifx\inserttitlegraphic\@empty\else\inserttitlegraphic\fi
		\vspace*{5mm}
\end{minipage}}

% this controls the width of the progress bar. x > 1.1 will make it fat x < 1.1 will make it thin
%\makeatletter
%\setlength{\metropolis@titleseparator@linewidth}{1.1pt}
%\setlength{\metropolis@progressonsectionpage@linewidth}{1.1pt}
%\setlength{\metropolis@progressinheadfoot@linewidth}{1.1pt}
%\makeatother

\setbeamertemplate{title}{
	\linespread{1.0}%
	\inserttitle%
	\par%
	\vspace*{0.5em}}
\setbeamertemplate{subtitle}{
	\insertsubtitle%
	\par%
	\vspace*{0.5em}}
\makeatother



% User: start from here --->
\title{\alert{\textbf{Can Public Preschools Level the Playing Field?}}}
\subtitle{Evidence From Andhra Pradesh}
\author{Zaeen de Souza}
\institute{Azim Premji University}
% User: update this link to photo below; replace \centering with \flushright or \flushleft to align left/right
%\titlegraphic{\centering\includegraphics[height=1cm]{apu.png}}
% un-comment below if you need to add > 1 logo
\titlegraphic{\centering\includegraphics[height=1cm]{apu.png}}



\begin{document}
\maketitle%
	% if you need an outline/index/something of that - comment/delete if not needed
	\begin{frame}{Outline}
		\small
		\setbeamertemplate{section in toc}[sections numbered]
		\tableofcontents[hideallsubsections]
	\end{frame}

% delete/comment out section titles if not needed
\section{Section 1 Heading}



\begin{frame}{Heading 1}
Something something something
\end{frame}

\section{Section 2 Heading}

\begin{frame}{Section 2}
		\alert{\textbf{Something bold}}
		\begin{itemize}
			\item[] \textbf{Main point 1} \\
			something something
			\begin{itemize}
				\item[-] Point 1
				\item[-] Point 2
			\end{itemize}
		\end{itemize}
		\alert{\textbf{Something bold}}
		\begin{itemize}
			\item[] \textbf{Main point 2} \\
			something something
			\begin{itemize}
				\item[-] Point 1
				\item[-] Point 2
			\end{itemize}
		\end{itemize}
\end{frame}




\section{Math}

\begin{frame}{This is some random math}

\begin{equation}
\frac{1}{n} \sum_{i=i}^{n} x_{i} \geq
\left(\prod _{i=1}^{n}x_{i}\right)^{\frac {1}{n}}
\end{equation}

\end{frame}



% anything after \appendix will not be included in slide numbers

\appendix

{
\setbeamercolor{palette primary}{bg = cseblue, fg = white}
\begin{frame}[standout]{}
		Extra Slides \& Appendix
	\end{frame}
}

%\subsection[References]{References}




\end{document}